edition = 3

[agent]
name = "solana-ct-alpha"
version = "0.1.0"
description = "Crypto Twitter intelligence — real-time narratives, trending tokens, yield strategies, smart money signals, TweetRank scoring, and raid detection."

readme = "Crypto Twitter intelligence — real-time narratives, trending tokens, yield strategies, smart money signals, TweetRank scoring, and raid detection."
summary = "Crypto Twitter intelligence — real-time narratives, trending tokens, yield strategies, smart money signals, TweetRank scoring, and raid detection."

[agent.license]
type = "sdpx"
expression = "MIT"

[runtimes.executable]
path = "bash"
arguments = ["startup.sh"]
transport = "streamable_http"

[options.OPENAI_API_KEY]
type = "string"
required = true
secret = true

[options.OPENAI_API_KEY.display]
description = "OpenAI API key for gpt-5.4-mini"
