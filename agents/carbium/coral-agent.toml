edition = 3

[agent]
name = "solana-carbium"
version = "0.1.0"
description = "Carbium infrastructure — bare-metal RPC, gRPC streaming (~22ms), DEX aggregation via CQ1 engine, gasless swaps, and MEV-protected execution."

readme = "Carbium infrastructure — bare-metal RPC, gRPC streaming (~22ms), DEX aggregation via CQ1 engine, gasless swaps, and MEV-protected execution."
summary = "Carbium infrastructure — bare-metal RPC, gRPC streaming (~22ms), DEX aggregation via CQ1 engine, gasless swaps, and MEV-protected execution."

[agent.license]
type = "sdpx"
expression = "MIT"

[runtimes.executable]
path = "bash"
arguments = ["startup.sh"]
transport = "streamable_http"

[options.OPENAI_API_KEY]
type = "string"
required = true
secret = true

[options.OPENAI_API_KEY.display]
description = "OpenAI API key for gpt-5.4-mini"
