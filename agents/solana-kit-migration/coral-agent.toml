edition = 3

[agent]
name = "solana-kit-migration"
version = "0.1.0"
description = "Migration guidance from @solana/web3.js v1 to @solana/kit — API mappings, edge cases, and SDK transition patterns."

readme = "Migration guidance from @solana/web3.js v1 to @solana/kit — API mappings, edge cases, and SDK transition patterns."
summary = "Migration guidance from @solana/web3.js v1 to @solana/kit — API mappings, edge cases, and SDK transition patterns."

[agent.license]
type = "sdpx"
expression = "MIT"

[runtimes.executable]
path = "bash"
arguments = ["startup.sh"]
transport = "streamable_http"

[options.OPENAI_API_KEY]
type = "string"
required = true
secret = true

[options.OPENAI_API_KEY.display]
description = "OpenAI API key for gpt-5.4-mini"
