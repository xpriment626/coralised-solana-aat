edition = 3

[agent]
name = "solana-sanctum"
version = "0.1.0"
description = "Sanctum liquid staking operations — LST swaps, SOL staking, Infinity pool, and liquidity infrastructure on Solana."

readme = "Sanctum liquid staking operations — LST swaps, SOL staking, Infinity pool, and liquidity infrastructure on Solana."
summary = "Sanctum liquid staking operations — LST swaps, SOL staking, Infinity pool, and liquidity infrastructure on Solana."

[agent.license]
type = "sdpx"
expression = "MIT"

[runtimes.executable]
path = "bash"
arguments = ["startup.sh"]
transport = "streamable_http"

[options.OPENAI_API_KEY]
type = "string"
required = true
secret = true

[options.OPENAI_API_KEY.display]
description = "OpenAI API key for gpt-5.4-mini"
