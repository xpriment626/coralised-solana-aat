edition = 3

[agent]
name = "solana-light-protocol"
version = "0.1.0"
description = "Light Protocol — ZK Compression for rent-free compressed tokens/PDAs, high-performance token standard (200x cheaper than SPL), and TypeScript SDK."

readme = "Light Protocol — ZK Compression for rent-free compressed tokens/PDAs, high-performance token standard (200x cheaper than SPL), and TypeScript SDK."
summary = "Light Protocol — ZK Compression for rent-free compressed tokens/PDAs, high-performance token standard (200x cheaper than SPL), and TypeScript SDK."

[agent.license]
type = "sdpx"
expression = "MIT"

[runtimes.executable]
path = "bash"
arguments = ["startup.sh"]
transport = "streamable_http"

[options.OPENAI_API_KEY]
type = "string"
required = true
secret = true

[options.OPENAI_API_KEY.display]
description = "OpenAI API key for gpt-5.4-mini"
