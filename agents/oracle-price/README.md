# oracle-price

Capability atom for Pyth oracle price checks.

Agent Kit actions:

- `PYTH_FETCH_PRICE`

Primary handoffs:

- `market-price` for CoinGecko comparison
- `token-info` when symbol or token identity is ambiguous
