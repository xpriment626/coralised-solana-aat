edition = 3

[agent]
name = "solana-switchboard"
version = "0.1.0"
description = "Switchboard oracle operations — price feeds, on-demand data, VRF randomness, and real-time streaming via Surge on Solana."

readme = "Switchboard oracle operations — price feeds, on-demand data, VRF randomness, and real-time streaming via Surge on Solana."
summary = "Switchboard oracle operations — price feeds, on-demand data, VRF randomness, and real-time streaming via Surge on Solana."

[agent.license]
type = "sdpx"
expression = "MIT"

[runtimes.executable]
path = "bash"
arguments = ["startup.sh"]
transport = "streamable_http"

[options.OPENAI_API_KEY]
type = "string"
required = true
secret = true

[options.OPENAI_API_KEY.display]
description = "OpenAI API key for gpt-5.4-mini"
