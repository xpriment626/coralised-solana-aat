# token-info

Capability atom for token identity and metadata lookup.

Agent Kit actions:

- `GET_COINGECKO_TOKEN_INFO_ACTION`

Primary handoffs:

- `market-price` for CoinGecko price data
- `oracle-price` for Pyth oracle checks
