edition = 3

[agent]
name = "solana-raydium"
version = "0.1.0"
description = "Raydium AMM and CLMM pool operations — swaps, liquidity provision, pool creation, LaunchLab token launches, and farming on Solana."

readme = "Raydium AMM and CLMM pool operations — swaps, liquidity provision, pool creation, LaunchLab token launches, and farming on Solana."
summary = "Raydium AMM and CLMM pool operations — swaps, liquidity provision, pool creation, LaunchLab token launches, and farming on Solana."

[agent.license]
type = "sdpx"
expression = "MIT"

[runtimes.executable]
path = "bash"
arguments = ["startup.sh"]
transport = "streamable_http"

[options.OPENAI_API_KEY]
type = "string"
required = true
secret = true

[options.OPENAI_API_KEY.display]
description = "OpenAI API key for gpt-5.4-mini"
