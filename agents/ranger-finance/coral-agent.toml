edition = 3

[agent]
name = "solana-ranger-finance"
version = "0.1.0"
description = "Ranger Finance perps aggregation — smart order routing across Drift, Flash, Adrena, and Jupiter perps on Solana."

readme = "Ranger Finance perps aggregation — smart order routing across Drift, Flash, Adrena, and Jupiter perps on Solana."
summary = "Ranger Finance perps aggregation — smart order routing across Drift, Flash, Adrena, and Jupiter perps on Solana."

[agent.license]
type = "sdpx"
expression = "MIT"

[runtimes.executable]
path = "bash"
arguments = ["startup.sh"]
transport = "streamable_http"

[options.OPENAI_API_KEY]
type = "string"
required = true
secret = true

[options.OPENAI_API_KEY.display]
description = "OpenAI API key for gpt-5.4-mini"
