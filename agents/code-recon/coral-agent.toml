edition = 3

[agent]
name = "solana-code-recon"
version = "0.1.0"
description = "Security audit preparation — deep architectural context building, trust boundary mapping, and Trail of Bits-inspired codebase analysis."

readme = "Security audit preparation — deep architectural context building, trust boundary mapping, and Trail of Bits-inspired codebase analysis."
summary = "Security audit preparation — deep architectural context building, trust boundary mapping, and Trail of Bits-inspired codebase analysis."

[agent.license]
type = "sdpx"
expression = "MIT"

[runtimes.executable]
path = "bash"
arguments = ["startup.sh"]
transport = "streamable_http"

[options.OPENAI_API_KEY]
type = "string"
required = true
secret = true

[options.OPENAI_API_KEY.display]
description = "OpenAI API key for gpt-5.4-mini"
