edition = 3

[agent]
name = "solana-coingecko"
version = "0.1.0"
description = "CoinGecko market data — token prices, DEX pool data, OHLCV charts, trades, and analytics for Solana trading applications."

readme = "CoinGecko market data — token prices, DEX pool data, OHLCV charts, trades, and analytics for Solana trading applications."
summary = "CoinGecko market data — token prices, DEX pool data, OHLCV charts, trades, and analytics for Solana trading applications."

[agent.license]
type = "sdpx"
expression = "MIT"

[runtimes.executable]
path = "bash"
arguments = ["startup.sh"]
transport = "streamable_http"

[options.OPENAI_API_KEY]
type = "string"
required = true
secret = true

[options.OPENAI_API_KEY.display]
description = "OpenAI API key for gpt-5.4-mini"
